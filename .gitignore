.venv/
__pycache__/
*.pyc
.env
node_modules/
scratch/
.claude/settings.local.json
!.claude/settings.json

# Stray clone the cloud-session sync drops here when applying a routine commit.
# Nested repo — without this, git add -A records it as a gitlink.
/dollar-experiment/

# Scratch downloads of other projects while researching a channel.
ap.md

# Per-cycle run logs from run-cycle.ps1.
logs/
