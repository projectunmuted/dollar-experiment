__pycache__/
*.pyc
.env

# Local Claude Code overrides stay local; the shared allowlist is tracked.
.claude/settings.local.json
!.claude/settings.json

# Per-cycle run logs from run-cycle.ps1 — local diagnostics only.
logs/
